Данный docker-compose разворачивает в докер контейнере GitLab Community Edition.\
В данном случае будет доступен в локальной сети в веб браузере по адресу http://192.168.1.50:8929 \
Чтобы поднять в фоновом режиме: docker-compose up -d.

